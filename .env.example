# -------------------------------
# Example Environment Variables
# -------------------------------

# Runtime environment for the app.
# Options: development, test, production
# Set manually depending on how/where the app runs.
NODE_ENV=development

# Port number the server will listen on.
# Default in schema is 9000.
# Choose a free port on your machine or hosting provider.
PORT=9000

# Logging verbosity level.
# Options: fatal, error, warn, info, debug, trace, silent
# "info" is common for production, "debug" for development.
LOG_LEVEL=info

# Database connection string.
# Format depends on your database type.
# Example (Postgres):
#   postgres://username:password@host:5432/dbname
# Source: Provided by your database provider or set up locally.
DATABASE_URL=postgres://user:pass@localhost:5432/mydb

# Optional database authentication token.
# Some managed DB services (e.g., Supabase, Prisma Data Proxy)
# require an auth token instead of username/password.
# Source: Generated in your DB provider’s dashboard.
DATABASE_AUTH_TOKEN=your-auth-token-here